from distutils.core import setup

setup(name='Pelican representative image plugin',
      version='0.1',
      url='https://github.com/linevich/representative_image',
      packages=['representative_image',],
)
